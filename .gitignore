#include <iostream> // Integrantes: Arthur Pereira, Eike Carlin, Paulo Hermans e Thyago Magalhaes
using namespace std;

int main()
{
    long long int codigo; // Se usa long long int para a entrada do codigo pois o numero requerido e muito longo.
    int D12,D11,D10,D9,D8,D7,D6,D5,D4,D3,D2,D1,D0,soma,x; // Sao criadas todas as variaveis para as operacoes matematicas seguintes.
    cout << "Digite um Codigo de Barras de treze digitos: "; // Comando para o pedido do codigo.
    cin >> codigo; // Comando para digitacao.
        if (codigo > 9999999999999 || codigo < 1000000000000) // Funcao que verifica se a quantidade de caracteres esta correta.
        {
            cout << "\nCodigo Incorreto.\nSeu codigo nao apresenta o numero correto de digitos.\n"; // Mensagem mostrada caso o codigo esteja com numero de caracteres incorreto.
        }
        else
        {
            D0 = codigo % 10;
            codigo = codigo / 10;
            D1 = codigo % 10;
            codigo = codigo / 10;
            D2 = codigo % 10;
            codigo = codigo / 10;
            D3 = codigo % 10;
            codigo = codigo / 10;
            D4 = codigo % 10;
            codigo = codigo / 10;
            D5 = codigo % 10;
            codigo = codigo / 10;
            D6 = codigo % 10;
            codigo = codigo / 10;
            D7 = codigo % 10;
            codigo = codigo / 10;
            D8 = codigo % 10;
            codigo = codigo / 10;
            D9 = codigo % 10;
            codigo = codigo / 10;
            D10 = codigo % 10;
            codigo = codigo / 10;
            D11 = codigo % 10;
            codigo = codigo / 10;
            D12 = codigo % 10;
            codigo = codigo / 10;
            soma = D12 + D11*3 + D10 + D9*3 + D8 + D7*3 + D6 + D5*3 + D4 + D3*3 + D2 + D1*3; // Operacoes para criacao do digito verificador correto.
            x = soma % 10;
            x = ((x-10) * (-1)) % 10;
                if (x == D0) // Funcao de verificacao da integridade do digito verificador.
                    {
                    cout << "\nCodigo Correto.\nSeu codigo de barras esta correto.\n"; // Mensagem em caso de codigo verificador correto.
                    }
                else
                    {
                    cout << "\nCodigo Incorreto.\nSeu numero apresenta o digito verificador incorreto.\nO digito verificador correto seria: " << x << ".\n"; // mensagem em caso de codigo verificador incorreto seguido da correcao.
                    }
        }
    return 0;
}
